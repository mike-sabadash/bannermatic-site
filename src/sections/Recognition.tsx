import { motion } from 'framer-motion'
const capabilities=[['Creative system','One idea, built to scale','01'],['Format coverage','Layouts adapted by aspect ratio','02'],['Motion system','Consistent timing across sizes','03'],['Production QA','Specs checked before delivery','04'],['Media-plan ready','Assets organized by placement','05'],['Campaign continuity','One visual language everywhere','06']]
export default function Recognition(){return <section className="px-6 md:px-10 py-24 md:py-36">
 <div className="flex items-baseline justify-between mb-14 md:mb-20"><span className="text-[11px] tracking-[0.25em] uppercase text-[#8a877f]">04 — Production</span><span className="text-[11px] tracking-[0.25em] uppercase text-[#8a877f]">Built for real campaigns</span></div>
 <div className="grid md:grid-cols-2 gap-16 md:gap-10"><div><h2 className="font-display font-medium text-[10vw] md:text-[4.5vw] leading-[1.02]">Creative that scales.<br/><span className="text-[#8a877f]">Without falling apart.</span></h2><p className="mt-8 max-w-md text-[#8a877f] leading-relaxed">Banner production is where a strong campaign often loses consistency. We design the system and the adaptations together, so every placement still feels like the same campaign.</p></div>
 <div>{capabilities.map(([org,name,count],i)=><motion.div key={name} initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-60px'}} transition={{duration:.6,delay:i*.06,ease:[.22,1,.36,1]}} className="group flex items-center justify-between border-t border-white/10 last:border-b py-5 md:py-6"><div><div className="font-display text-lg md:text-2xl font-medium transition-colors duration-300 group-hover:text-[#d7ff3f]">{org}</div><div className="text-[12px] text-[#8a877f] mt-1">{name}</div></div><span className="font-display text-lg md:text-xl text-[#8a877f] tabular-nums">{count}</span></motion.div>)}</div></div>
 </section>}
